import { TextFizzleDirective } from './text-fizzle.directive';

describe('TextFizzleDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFizzleDirective();
    expect(directive).toBeTruthy();
  });
});
